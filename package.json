{
  "name": "cloakpack-monorepo",
  "private": true,
  "description": "One product, one vault, two gates: the cloakpack git pre-push guard (CLI) and the dsh-cloak context firewall for DeepSeek Harness, sharing a single secret-detection engine.",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "license": "MIT"
}
